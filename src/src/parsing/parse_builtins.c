#include "../../minishell.h"

/* ---------------------- 1.Parsing temp ----------------------------*/
int	parse_builtins(t_shell *info)
{
	(void)info;
	//if (ft_strncmp(*info->arg, "echo", 5) == 0)
	//	echo_minishell(info->arg + 2);
	// else if (ft_strncmp(*info->arg, "exit", 5) == 0)
	// 	exit_minishell(info);
	// else if (ft_strncmp(*info->arg, "pwd", 4) == 0)
	// 	pwd_minishell(info);
	// else if (ft_strncmp(*info->arg, "env", 4) == 0)
	// 	env_minishell(info);
	// else if (ft_strncmp(*info->arg, "cd", 3) == 0)
	// 	cd_minishell(info);
	// else if (ft_strncmp(*info->arg, "export", 7) == 0)
	// 	export_minishell(info);
	// else if (ft_strncmp(*info->arg, "unset", 6) == 0)
	// 	unset_minishell(&info);
	//else
	//	return (1);
	return (0);
}
